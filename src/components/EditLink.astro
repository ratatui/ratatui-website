---
// This component intentionally renders nothing. It overrides Starlight's
// default EditLink to suppress the "Edit page" link at the bottom of the page.
// PageActions.astro renders the link next to the page title instead; without
// this empty override, the link would appear in both places.
---
