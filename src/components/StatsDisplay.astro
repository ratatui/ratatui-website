---
// Component to display dynamic stats with fallbacks
import { getAllStats } from "../utils/stats";

let stats;
try {
  console.log("Fetching stats at build time...");
  stats = await getAllStats();
  console.log("Successfully fetched stats:", stats);
} catch (error) {
  console.error("Failed to load stats, using fallbacks:");
  console.error("Error type:", error?.constructor?.name);
  console.error("Error message:", (error as Error)?.message || "Unknown error");
  console.error("Full error:", error);
  stats = {
    crates: "1000+",
    stars: "13.4k",
    downloads: "7.1M",
  };
}
---

<div class="pt-[60px] text-xl landing-mono">
  <p class="landing-mono">
    <span class="text-slate-300">
      <a
        class="no-underline"
        href="https://crates.io/crates/ratatui/reverse_dependencies"
        target="_blank"
        rel="noopener noreferrer"
      >
        <span class="font-bold text-black dark:text-white bg-slate-100 dark:bg-slate-900 p-2 px-6 m-2 landing-mono">{stats.crates}</span>
      </a> 📦 crates built with Ratatui
    </span>
    <span class="text-slate-300">
      <a class="no-underline" href="https://github.com/ratatui-org/ratatui" target="_blank" rel="noopener noreferrer">
        <span class="font-bold text-black dark:text-white bg-slate-100 dark:bg-slate-900 p-2 px-6 m-2 landing-mono">{stats.stars}</span>
      </a> ⭐ stars on GitHub
    </span>
    <span class="text-slate-300">
      <a class="no-underline" href="https://crates.io/crates/ratatui" target="_blank" rel="noopener noreferrer">
        <span class="font-bold text-black dark:text-white bg-slate-100 dark:bg-slate-900 p-2 px-6 m-2 landing-mono">{stats.downloads}</span>
      </a> ⬇️ downloads on crates.io
    </span>
  </p>
</div>
